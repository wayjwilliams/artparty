<div class="row">
  <div class="col-md-6 col-md-offset-3 text-center">
    <% if params[:plan] == '2' %>
      <h1>Premium Account</h1>
      <p>Just $10/month gets you behind the velvet rope for all that VIP Goodness </p>
    <% else %>
      <h1> Basic Account </h1>
      <p>We get it. Times are tough! Ain't nobody got time to spend $10/month!!!</p>
    <% end %>
  </div>
  <div class="col-md-6 col-md-offset-3">
    <div class="well">
    <h2>Sign up</h2>

      <% if params[:plan] == '2' %>
        <%= render "premium_form" %>
      <% else %>
        <%= render "basic_form" %>
      <% end %>

      <%= render "devise/shared/links" %>

    </div>
  </div>
</div>
